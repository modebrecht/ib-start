name: One-time navigation icon polish

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/one-time-fix-nav-icons.yml'

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - name: Apply navigation icon polish
        run: python .github/scripts/fix-nav-icons.py
      - name: Check JavaScript syntax
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          text = Path('index.html').read_text(encoding='utf-8')
          scripts = re.findall(r'<script>(.*?)</script>', text, flags=re.S)
          if not scripts:
              raise SystemExit('No script block found')
          Path('/tmp/app.js').write_text(scripts[-1], encoding='utf-8')
          PY
          node --check /tmp/app.js
      - name: Commit final change and remove one-time tooling
        run: |
          rm -f .github/scripts/fix-nav-icons.py
          rm -f .github/workflows/one-time-fix-nav-icons.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/scripts .github/workflows
          git commit -m "fix: polish navigation icons"
          git push origin HEAD:dev
