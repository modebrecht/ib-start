<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Meine Kurzvorstellung – Matthias</title>
<style>
*{box-sizing:border-box;margin:0;padding:0}
body{font-family:Arial,Helvetica,sans-serif;background:#080d1c;color:#fff;overflow:hidden}
.presentation{width:100vw;height:100vh;position:relative;background:radial-gradient(circle at 15% 20%,#5146d9 0%,transparent 30%),radial-gradient(circle at 85% 80%,#006b91 0%,transparent 30%),#080d1c}
.slide{position:absolute;inset:0;padding:7vh 6vw 11vh;display:flex;flex-direction:column;justify-content:center;opacity:0;visibility:hidden;transform:translateX(40px);transition:.5s ease}
.slide.active{opacity:1;visibility:visible;transform:translateX(0)}
.number{position:absolute;top:4vh;right:5vw;color:#aab4cc;font-size:1rem}
.eyebrow{color:#00d9ff;text-transform:uppercase;letter-spacing:4px;font-weight:700;margin-bottom:18px}
h1{font-size:clamp(4rem,10vw,9rem);line-height:.95;margin-bottom:25px}
h2{font-size:clamp(2.7rem,5.2vw,5.4rem);margin-bottom:28px;line-height:1.02}
.gradient{background:linear-gradient(90deg,#00d9ff,#8b70ff,#ff65c7);-webkit-background-clip:text;color:transparent}
.subtitle{color:#cbd5e1;font-size:clamp(1.2rem,2.3vw,2rem);line-height:1.5;max-width:980px}

.scene-grid{display:grid;gap:16px;align-items:stretch}
.hobby-grid{grid-template-columns:repeat(5,1fr)}
.animal-grid{grid-template-columns:repeat(4,1fr)}
.learning-grid{grid-template-columns:repeat(2,1fr);max-width:1180px;gap:20px}
.scene-card{min-width:0;padding:16px 16px 18px;border-radius:24px;background:linear-gradient(145deg,rgba(255,255,255,.12),rgba(255,255,255,.055));border:1px solid rgba(255,255,255,.16);backdrop-filter:blur(12px);box-shadow:0 14px 50px rgba(0,0,0,.17);overflow:hidden;position:relative}
.scene-card::before{content:"";position:absolute;inset:-40% auto auto -20%;width:100px;height:100px;background:#6b5cff;filter:blur(55px);opacity:.28;pointer-events:none}
.scene-card h3{font-size:clamp(1.35rem,1.55vw,1.8rem);margin:8px 0 5px}
.scene-card p{color:#e0e7f3;font-size:clamp(1.05rem,1.18vw,1.25rem);line-height:1.22;font-weight:500}
.scene-svg{width:100%;height:152px;display:block;overflow:visible}
.animal-grid .scene-svg{height:206px}
.learning-grid .scene-svg{height:185px}
.section-note{color:#a9b7d2;font-size:1rem;margin:-10px 0 18px;max-width:1050px;line-height:1.35}
.card-number{position:absolute;top:16px;left:18px;min-width:34px;height:34px;padding:0 10px;border-radius:999px;display:inline-flex;align-items:center;justify-content:center;background:linear-gradient(90deg,#00d9ff,#8b70ff);color:#08101d;font-weight:800;font-size:.95rem;z-index:2}.learning-grid .scene-card{padding:18px 20px 22px}.learning-grid .scene-card h3{font-size:clamp(1.25rem,1.6vw,1.8rem);margin:8px 0 5px}.learning-grid .scene-card p{font-size:clamp(.92rem,1vw,1.05rem);line-height:1.3;color:#d4def1}.learning-grid .scene-card .scene-svg{margin-top:8px}


.project-grid .scene-svg{height:185px}
.project-grid .scene-card h3{font-size:clamp(1.35rem,1.55vw,1.8rem)}
.project-grid .scene-card p{font-size:clamp(1.08rem,1.2vw,1.28rem);line-height:1.2}
.hobby-grid .scene-card{padding:14px 14px 16px}
.hobby-grid .scene-svg{height:168px}
.animal-grid .scene-card{padding:14px 14px 16px}
.learning-grid .scene-card{padding:14px 14px 16px}

/* SVG animation helpers */
.float{animation:float 3s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.float2{animation:float 3.8s ease-in-out infinite reverse;transform-box:fill-box;transform-origin:center}
.bob{animation:bob 1.7s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.spin{animation:spin 5s linear infinite;transform-box:fill-box;transform-origin:center}
.spinSlow{animation:spin 10s linear infinite;transform-box:fill-box;transform-origin:center}
.pulse{animation:pulse 1.9s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.spark{animation:spark 1.6s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.swing{animation:swing 1.35s ease-in-out infinite alternate;transform-box:fill-box;transform-origin:85% 85%}
.slideBall{animation:slideBall 2s ease-in-out infinite}
.page{animation:page 2.7s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.dice{animation:dice 2.2s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.controller{animation:controller 1.8s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.codeLine{animation:codeLine 2s ease-in-out infinite alternate;transform-origin:left center;transform-box:fill-box}
.tail{animation:tail 1.8s ease-in-out infinite alternate;transform-box:fill-box;transform-origin:10% 80%}
.glide{animation:glide 3.5s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.waddle{animation:waddle .9s ease-in-out infinite alternate;transform-box:fill-box;transform-origin:50% 90%}
.pathDash{stroke-dasharray:8 8;animation:dash 1.2s linear infinite}
.taskFly{animation:taskFly 3s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.robotHop{animation:robotHop 1.7s ease-in-out infinite;transform-box:fill-box;transform-origin:center}
.checkPop{animation:checkPop 1.8s ease-in-out infinite;transform-box:fill-box;transform-origin:center}

@keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}
@keyframes bob{0%,100%{transform:translateY(0)}50%{transform:translateY(-5px)}}
@keyframes spin{to{transform:rotate(360deg)}}
@keyframes pulse{0%,100%{transform:scale(1);opacity:.85}50%{transform:scale(1.13);opacity:1}}
@keyframes spark{0%,100%{transform:scale(.7) rotate(0);opacity:.4}50%{transform:scale(1.25) rotate(22deg);opacity:1}}
@keyframes swing{from{transform:rotate(-8deg)}to{transform:rotate(15deg)}}
@keyframes slideBall{0%{transform:translate(-20px,12px)}50%{transform:translate(30px,-20px)}100%{transform:translate(72px,8px)}}
@keyframes page{0%,100%{transform:translate(0,0) rotate(-7deg)}50%{transform:translate(7px,-10px) rotate(7deg)}}
@keyframes dice{0%,100%{transform:rotate(-7deg) translateY(0)}50%{transform:rotate(12deg) translateY(-8px)}}
@keyframes controller{0%,100%{transform:translateY(0) rotate(-2deg)}50%{transform:translateY(-5px) rotate(2deg)}}
@keyframes codeLine{from{transform:scaleX(.55)}to{transform:scaleX(1)}}
@keyframes tail{from{transform:rotate(-8deg)}to{transform:rotate(11deg)}}
@keyframes glide{0%,100%{transform:translate(-4px,6px) rotate(-3deg)}50%{transform:translate(10px,-8px) rotate(4deg)}}
@keyframes waddle{from{transform:rotate(-3deg) translateY(0)}to{transform:rotate(3deg) translateY(-3px)}}
@keyframes dash{to{stroke-dashoffset:-32}}
@keyframes taskFly{0%,100%{transform:translateX(-5px)}50%{transform:translateX(10px)}}
@keyframes robotHop{0%,100%{transform:translateY(0) rotate(-2deg)}50%{transform:translateY(-8px) rotate(3deg)}}
@keyframes checkPop{0%,100%{transform:scale(.9)}50%{transform:scale(1.12)}}

.nav-icon{width:22px;height:22px;display:block;margin:auto}
.navigation{position:fixed;left:50%;bottom:1.8vh;transform:translateX(-50%);display:flex;align-items:center;gap:10px;z-index:20;opacity:.72;transition:opacity .2s ease}
button{width:46px;height:46px;border-radius:50%;border:1px solid rgba(255,255,255,.16);background:rgba(255,255,255,.08);color:#fff;font-size:1.25rem;cursor:pointer;backdrop-filter:blur(8px)}
.navigation:hover{opacity:1}button:hover{background:#6257ff}.dots{display:flex;gap:7px}.dot{width:9px;height:9px;border-radius:50%;background:#596174}.dot.active{width:28px;background:#00d9ff}.fullscreen{position:fixed;top:3vh;left:4vw;z-index:30;width:45px;height:45px}.hint{display:none}


.learning-slide{
  padding:5vh 5vw 9vh;
  justify-content:center;
}
.learning-slide h2{
  font-size:clamp(2.4rem,4.2vw,4.6rem);
  margin-bottom:18px;
  line-height:1;
}
.learning-slide .learning-grid{
  gap:16px;
}
.learning-slide .scene-card{
  padding:11px 15px 13px;
}
.learning-slide .scene-svg{
  height:165px;
  margin-top:2px;
}
.learning-slide .scene-card h3{
  font-size:clamp(1.38rem,1.5vw,1.72rem);
  margin:5px 0 3px;
}
.learning-slide .scene-card p{
  font-size:clamp(1.08rem,1.16vw,1.24rem);
  line-height:1.18;
  font-weight:500;
}
.learning-slide .card-number{
  top:12px;
  left:14px;
  min-width:32px;
  height:32px;
  font-size:1.05rem;
}

@media(max-width:1100px){.hobby-grid{grid-template-columns:repeat(3,1fr)}.animal-grid,.learning-grid{grid-template-columns:repeat(2,1fr)}.scene-card{padding:13px}.scene-svg{height:126px}.hobby-grid .scene-svg{height:140px}.project-grid .scene-svg{height:155px}.animal-grid .scene-svg{height:150px}.learning-grid .scene-svg{height:145px}.learning-slide{padding:4vh 4vw 9vh}.learning-slide h2{font-size:clamp(2.2rem,4vw,4rem)}}
@media(max-width:800px){.slide{padding:8vh 5vw 12vh;justify-content:flex-start;overflow:auto}.scene-grid{grid-template-columns:1fr;max-height:68vh;overflow:auto}.learning-grid{grid-template-columns:1fr}.scene-svg{height:140px}.hobby-grid .scene-svg{height:152px}.project-grid .scene-svg{height:170px}.animal-grid .scene-svg{height:175px}.learning-grid .scene-svg{height:170px}.hint{display:none}}


/* ---- Zusätzliche Abschlussfolien ---- */
.alt-learning-slide{
  padding:5vh 6vw 9vh;
  justify-content:center;
}
.alt-learning-slide h2{
  font-size:clamp(2.5rem,4.4vw,4.8rem);
  margin-bottom:18px;
}
.alt-map{
  position:relative;
  width:min(1180px,92vw);
  height:min(660px,70vh);
  margin:0 auto;
  border-radius:34px;
  background:
    radial-gradient(circle at 50% 50%,rgba(0,217,255,.13),transparent 29%),
    linear-gradient(145deg,rgba(255,255,255,.08),rgba(255,255,255,.035));
  border:1px solid rgba(255,255,255,.14);
  overflow:hidden;
}
.alt-map svg{
  width:100%;
  height:100%;
  display:block;
}
.alt-node-title{
  font:700 25px Arial,Helvetica,sans-serif;
  fill:white;
}
.alt-node-sub{
  font:600 17px Arial,Helvetica,sans-serif;
  fill:#dce7f5;
}
.alt-center-title{
  font:800 32px Arial,Helvetica,sans-serif;
  fill:white;
}
.alt-center-sub{
  font:600 18px Arial,Helvetica,sans-serif;
  fill:#d8e8f8;
}
.orbit-dash{
  stroke-dasharray:10 10;
  animation:dash 1.5s linear infinite;
}
.flow-dot{
  animation:flowDot 3.4s ease-in-out infinite;
  transform-box:fill-box;
  transform-origin:center;
}
@keyframes flowDot{
  0%,100%{transform:translate(0,0);opacity:.35}
  50%{transform:translate(8px,-7px);opacity:1}
}

/* HARD CUT before the picture section */
.cut-slide{
  padding:0;
  justify-content:center;
  align-items:center;
  background:
    radial-gradient(circle at 50% 50%, rgba(0,217,255,.08), transparent 30%),
    linear-gradient(180deg,#02050e 0%,#050a18 100%);
}
.cut-slide .cut-wrap{
  width:100%;
  height:100%;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:18px;
}
.cut-slide .cut-line{
  width:min(74vw,980px);
  height:2px;
  background:linear-gradient(90deg,transparent,#00d9ff,#8b70ff,#ff65c7,transparent);
  box-shadow:0 0 22px rgba(0,217,255,.45);
}
.cut-slide .cut-title{
  font-size:clamp(3.4rem,8.2vw,7.4rem);
  font-weight:800;
  line-height:.92;
  text-align:center;
  letter-spacing:-.03em;
}
.cut-slide .cut-sub{
  color:#b8c6de;
  font-size:clamp(1.05rem,1.7vw,1.45rem);
  text-align:center;
}

/* IMAGE SECTION: image first, text minimal */
.image-slide{
  padding:1.8vh 2vw 4.5vh;
  justify-content:center;
  background:
    radial-gradient(circle at 50% 50%, rgba(10,18,34,.45), transparent 60%),
    linear-gradient(180deg, rgba(0,0,0,.10), rgba(0,0,0,.12));
}
.image-slide .eyebrow,
.image-slide h2{
  display:none;
}
.hero-image-wrap{
  width:min(98vw,1700px);
  height:min(88vh,980px);
  margin:0 auto;
  position:relative;
  display:flex;
  align-items:center;
  justify-content:center;
}
.hero-image{
  display:block;
  width:100%;
  height:100%;
  object-fit:contain;
  border-radius:30px;
  border:1px solid rgba(255,255,255,.14);
  box-shadow:0 26px 80px rgba(0,0,0,.34);
  background:rgba(4,9,20,.45);
}
.image-